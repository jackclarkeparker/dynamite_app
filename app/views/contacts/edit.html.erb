<h1>Editing contact</h1>

<% if @render_failed_edit %>
  <%= render "failed_edit", contact: @contact, original_id: @original_id %>
<% else %>
  <%= render "form", contact: @contact %>
<% end %>

<br>

<div>
  <%= link_to "Show this contact", @contact %> |
  <%= link_to "Back to contacts", contacts_path %>
</div>
